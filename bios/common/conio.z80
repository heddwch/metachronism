	MACLIB	IOREGS.INC
	GLOBAL	PUTCHAR,GETCHAR

PUTCHAR:
	LD	B, A
POLL_WRITE:
	IN	A, (STDIO_CONTROL)
	AND	STDIO_RDY_WRITE
	JP	Z, POLL_WRITE
	LD	A, B
	OUT	(STDIO_DATA), A
	RET

GETCHAR:
	IN	A, (STDIO_CONTROL)
	AND	STDIO_RDY_READ
	JP	Z, GETCHAR
	IN	A, (STDIO_DATA)
	RET

